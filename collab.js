(function(){
  const initial={role:'customer',privateNotes:[],questionnaire:null,requirement:null,implementation:null,code:null};
  let collab=JSON.parse(localStorage.getItem('erp-ai-collab')||'null')||structuredClone(initial);
  const roles={customer:'客户',consultant:'实施顾问',developer:'开发人员'};
  const roleTips={customer:'您可以与AI私密沟通，确认后再把需求方案发送给实施顾问。',consultant:'您只能看到客户确认后的需求方案，可整理实施文档并发送给开发人员。',developer:'您可以查看已确认需求和实施文档，并交付开发代码。'};
  const esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  function store(){localStorage.setItem('erp-ai-collab',JSON.stringify(collab));render()}
  function artifactCard(kind,title,item,action){return `<article class="delivery-card ${item?'ready':''}"><div class="delivery-icon">${item?'✓':'○'}</div><div><small>${kind}</small><b>${title}</b><span>${item?'已交付 · '+item.time:'等待上一步完成'}</span></div>${item?`<button class="secondary" data-collab="${action}">查看</button>`:'<button disabled>待交付</button>'}</article>`}
  function sharedTimeline(){let blocks=[];if(collab.requirement)blocks.push(`<div class="handoff customer"><div class="handoff-meta"><b>客户</b><span>发送给实施顾问 · ${collab.requirement.time}</span><em>客户与顾问可见</em></div><div class="handoff-card"><span class="doc-type">需求方案</span><h3>${esc(collab.requirement.title)}</h3><p>${esc(collab.requirement.content)}</p><div class="handoff-actions"><button class="secondary" data-collab="viewRequirement">查看完整需求</button>${collab.role==='consultant'&&!collab.implementation?'<button class="primary" data-collab="draftImplementation">编写实施文档</button>':''}</div></div></div>`);else blocks.push('<div class="collab-empty"><b>等待客户提交需求方案</b><span>客户与AI的私密沟通过程不会在这里显示。</span></div>');if(collab.implementation)blocks.push(`<div class="handoff consultant"><div class="handoff-meta"><b>实施顾问</b><span>发送给开发人员 · ${collab.implementation.time}</span><em>三方可见</em></div><div class="handoff-card"><span class="doc-type">实施文档</span><h3>${esc(collab.implementation.title)}</h3><p>${esc(collab.implementation.content)}</p><div class="handoff-actions"><button class="secondary" data-collab="viewImplementation">查看实施文档</button>${collab.role==='developer'&&!collab.code?'<button class="primary" data-collab="deliverCode">开始开发交付</button>':''}</div></div></div>`);if(collab.code)blocks.push(`<div class="handoff developer"><div class="handoff-meta"><b>开发人员</b><span>完成交付 · ${collab.code.time}</span><em>三方可见</em></div><div class="handoff-card code-card"><span class="doc-type">开发代码</span><h3>${esc(collab.code.title)}</h3><p>${esc(collab.code.content)}</p><button class="secondary" data-collab="viewCode">查看代码交付</button></div></div>`);return blocks.join('')}
  function privateArea(){if(collab.role!=='customer')return `<div class="privacy-notice"><b>客户与AI的沟通细节已隐藏</b><p>${roleTips[collab.role]}</p><span>您将在客户确认后收到结构化的需求方案。</span></div>`;let form=collab.questionnaire?`<div class="ai-form-card"><div class="ai-form-title"><div><b>请补充业务规则</b><span>AI已把需要确认的内容整理成表格，请逐项填写</span></div><em>${collab.questionnaire.completed?'已填写':'待填写'}</em></div><div class="ai-table"><label><b>表单字段 *</b><span>页面需要客户填写哪些内容？</span><textarea id="qFields" rows="2" placeholder="例如：申请人、部门、金额、供应商、申请原因">${esc(collab.questionnaire.fields||'')}</textarea></label><label><b>审批人员 *</b><span>提交后需要经过哪些人审批？</span><input id="qApprovers" placeholder="例如：部门经理、财务总监、总经理" value="${esc(collab.questionnaire.approvers||'')}"></label><label><b>金额条件</b><span>不同金额是否走不同审批流程？</span><input id="qAmount" placeholder="例如：超过5万元增加财务总监审批" value="${esc(collab.questionnaire.amount||'')}"></label><label><b>驳回规则</b><span>审批不通过时退回到哪里？</span><select id="qReject"><option ${collab.questionnaire.reject==='退回发起人'?'selected':''}>退回发起人</option><option ${collab.questionnaire.reject==='退回上一审批人'?'selected':''}>退回上一审批人</option><option ${collab.questionnaire.reject==='退回指定节点'?'selected':''}>退回指定节点</option></select></label><label><b>超时规则</b><span>审批超时后如何处理？</span><div class="inline-input"><input id="qTimeout" type="number" min="1" placeholder="24" value="${esc(collab.questionnaire.timeout||'')}"><span>小时后</span><select id="qTimeoutAction"><option ${collab.questionnaire.timeoutAction==='提醒审批人'?'selected':''}>提醒审批人</option><option ${collab.questionnaire.timeoutAction==='升级给上级'?'selected':''}>升级给上级</option><option ${collab.questionnaire.timeoutAction==='自动通过'?'selected':''}>自动通过</option></select></div></label></div><div class="ai-form-actions"><span>带 * 的项目填写后即可生成需求摘要</span><button class="primary" data-collab="saveQuestionnaire">保存并生成需求摘要</button></div></div>`:'';return `<div class="private-ai"><div class="private-head"><div><b>客户 × AI 私密需求整理</b><span>仅客户本人可见，实施顾问和开发人员看不到此处内容</span></div><em>私密</em></div><div class="private-log">${collab.privateNotes.length?collab.privateNotes.map(n=>`<div><b>${n.role==='customer'?'客户':'AI'}</b><p>${esc(n.text)}</p></div>`).join(''):'<div class="private-welcome"><b>先用自己的话说明需求</b><p>例如：采购金额超过5万元需要财务总监审批，超过20万元还要总经理审批。</p></div>'}</div>${form}<div class="private-compose"><textarea id="privateInput" rows="2" placeholder="输入业务想法、规则或需要解决的问题"></textarea><button class="primary" data-collab="askAI">发送给AI</button></div><div class="publish-box"><div><b>确认后再对外发送</b><span>完成结构化表格后，AI会生成正式需求方案，只有确认后的内容会发送给实施顾问。</span></div><button class="primary" data-collab="publishRequirement">生成并发送需求方案</button></div></div>`}
  function render(){const root=document.querySelector('#assistant');if(!root)return;root.innerHTML=`<div class="toolbar"><div><h2>三方协作与交付中心</h2><p>客户、实施顾问和开发人员按阶段传递已确认成果。</p></div><div class="role-switch">${Object.entries(roles).map(([k,v])=>`<button data-role-switch="${k}" class="${collab.role===k?'active':''}">${v}视角</button>`).join('')}</div></div><div class="visibility-banner"><b>当前身份：${roles[collab.role]}</b><span>${roleTips[collab.role]}</span></div><div class="collab-layout"><aside class="collab-rail"><h3>协作流程</h3><div class="stage ${collab.requirement?'done':'active'}"><i>${collab.requirement?'✓':'1'}</i><span><b>客户确认需求</b><small>私密AI沟通 → 需求方案</small></span></div><div class="stage ${collab.implementation?'done':collab.requirement?'active':''}"><i>${collab.implementation?'✓':'2'}</i><span><b>顾问制定实施方案</b><small>需求方案 → 实施文档</small></span></div><div class="stage ${collab.code?'done':collab.implementation?'active':''}"><i>${collab.code?'✓':'3'}</i><span><b>开发人员完成交付</b><small>实施文档 → 开发代码</small></span></div><div class="participant-list"><h4>项目参与者</h4><div><span class="p customer">客</span><b>客户</b><small>${collab.role==='customer'?'当前在线':'可查看交付'}</small></div><div><span class="p consultant">顾</span><b>实施顾问</b><small>${collab.requirement?'已接收需求':'等待需求'}</small></div><div><span class="p developer">开</span><b>开发人员</b><small>${collab.implementation?'已接收实施文档':'等待实施文档'}</small></div><div><span class="p ai">AI</span><b>AI助手</b><small>辅助各角色整理</small></div></div></aside><main class="collab-main"><div class="collab-tabs"><button class="active">协作对话</button><span>只显示已确认并发送的成果</span></div>${privateArea()}<div class="shared-head"><h3>项目协作对话</h3><span>阶段成果在这里交接</span></div><div class="handoff-list">${sharedTimeline()}</div></main><aside class="delivery-panel"><div class="delivery-head"><h3>客户最终交付</h3><span>三项成果统一查看</span></div>${artifactCard('01','需求',collab.requirement,'viewRequirement')}${artifactCard('02','实施文档',collab.implementation,'viewImplementation')}${artifactCard('03','开发代码',collab.code,'viewCode')}<div class="delivery-note"><b>客户可见范围</b><p>客户可以查看三项最终成果，但不会看到顾问与AI、开发人员与AI的内部整理过程。</p></div></aside></div>`}
  function now(){return new Date().toLocaleString()}
  function showDoc(title,body,type){modal(`<div class="modal-head"><div><h2>${title}</h2><p>${type} · 三方协作交付物</p></div><button data-close>×</button></div><div class="document-preview"><h3>${title}</h3><p>${body}</p><h4>交付状态</h4><p>已确认并进入下一阶段。当前为前端演示内容，正式版本将由AI整理和负责人审核后生成。</p></div><div class="modal-actions"><button class="secondary" data-close>关闭</button><button class="primary" onclick="toast('已下载交付物（演示）')">下载</button></div>`)}
  document.addEventListener('click',e=>{let role=e.target.closest('[data-role-switch]');if(role){collab.role=role.dataset.roleSwitch;store();return}let b=e.target.closest('[data-collab]');if(!b)return;let a=b.dataset.collab;if(a==='askAI'){let input=document.querySelector('#privateInput'),text=input.value.trim();if(!text)return toast('请输入需要梳理的业务内容');collab.privateNotes.push({role:'customer',text},{role:'ai',text:'我已记录。还需要确认涉及的表单字段、审批人员、金额条件、驳回和超时规则。确认完整后，可生成正式需求方案。'});store()}if(a==='publishRequirement'){let notes=collab.privateNotes.filter(n=>n.role==='customer').map(n=>n.text);if(!notes.length)return toast('请先与AI说明业务需求');collab.requirement={title:'业务需求确认方案',content:notes.join('；'),time:now()};store();toast('需求方案已发送给实施顾问')}if(a==='draftImplementation'){modal(`<div class="modal-head"><div><h2>编写实施文档</h2><p>基于已确认需求，形成开发人员可执行的实施说明。</p></div><button data-close>×</button></div><label class="field"><span>实施文档标题</span><input id="implTitle" value="业务应用实施方案"></label><label class="field"><span>实施内容</span><textarea id="implBody" rows="7">配置数据模型、业务表单和审批流程；完成权限配置、测试验证与用户确认。</textarea></label><div class="modal-actions"><button class="secondary" data-close>取消</button><button class="primary" id="sendImplementation">发送给开发人员</button></div>`);document.querySelector('#sendImplementation').onclick=()=>{collab.implementation={title:document.querySelector('#implTitle').value,content:document.querySelector('#implBody').value,time:now()};localStorage.setItem('erp-ai-collab',JSON.stringify(collab));closeModal();render();toast('实施文档已发送给开发人员')}}if(a==='deliverCode'){collab.code={title:'业务应用源代码与配置',content:'前端页面、审批流程配置、数据模型脚本、接口说明及部署说明。',time:now()};store();toast('开发代码已完成交付')}if(a==='viewRequirement'&&collab.requirement)showDoc(collab.requirement.title,collab.requirement.content,'需求');if(a==='viewImplementation'&&collab.implementation)showDoc(collab.implementation.title,collab.implementation.content,'实施文档');if(a==='viewCode'&&collab.code)showDoc(collab.code.title,collab.code.content,'开发代码')});
  document.addEventListener('click',e=>{let b=e.target.closest('[data-collab]');if(!b||!['askAI','saveQuestionnaire','publishRequirement'].includes(b.dataset.collab))return;e.preventDefault();e.stopImmediatePropagation();let action=b.dataset.collab;if(action==='askAI'){let input=document.querySelector('#privateInput'),text=input.value.trim();if(!text)return toast('请输入需要梳理的业务内容');collab.privateNotes.push({role:'customer',text},{role:'ai',text:'我已经理解您的初步需求。请填写下面的业务规则表，系统会据此生成正式需求摘要。'});collab.questionnaire=collab.questionnaire||{fields:'',approvers:'',amount:'',reject:'退回发起人',timeout:'24',timeoutAction:'提醒审批人',completed:false};store()}if(action==='saveQuestionnaire'){let fields=document.querySelector('#qFields').value.trim(),approvers=document.querySelector('#qApprovers').value.trim();if(!fields||!approvers)return toast('请至少填写表单字段和审批人员');collab.questionnaire={fields,approvers,amount:document.querySelector('#qAmount').value.trim(),reject:document.querySelector('#qReject').value,timeout:document.querySelector('#qTimeout').value.trim(),timeoutAction:document.querySelector('#qTimeoutAction').value,completed:true};collab.privateNotes.push({role:'ai',text:'结构化业务规则已经填写完成，正式需求摘要已生成。请确认后发送给实施顾问。'});store();toast('需求摘要已生成')}if(action==='publishRequirement'){let q=collab.questionnaire;if(!q?.completed)return toast('请先填写并保存业务规则表');let opening=collab.privateNotes.find(n=>n.role==='customer')?.text||'业务流程需求';collab.requirement={title:'业务需求确认方案',content:`业务场景：${opening}；表单字段：${q.fields}；审批人员：${q.approvers}；金额条件：${q.amount||'无特殊条件'}；驳回规则：${q.reject}；超时规则：${q.timeout||'未设置'}小时后${q.timeoutAction}`,time:now()};store();toast('需求方案已发送给实施顾问')}},true);
  window.renderResearch=render;render();
})();
